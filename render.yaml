services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: API_TOKEN
        fromService: my-secrets-service
    branch: main  # Make sure this is pointing to the correct branch
